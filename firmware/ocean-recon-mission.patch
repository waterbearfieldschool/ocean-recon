diff --git a/examples/companion_radio/MyMesh.cpp b/examples/companion_radio/MyMesh.cpp
index 6fbb0f7..b4f95ed 100644
--- a/examples/companion_radio/MyMesh.cpp
+++ b/examples/companion_radio/MyMesh.cpp
@@ -107,6 +107,7 @@
 #define LAZY_CONTACTS_WRITE_DELAY       5000
 
 #define PUBLIC_GROUP_PSK                "izOH6cXN6mrJ5e26oRXNcg=="
+#define EDGEWOOD_GROUP_PSK              "a5i7US4uIXqX5pO/z0vFww=="  // Ocean Recon mission channel
 
 // these are _pushed_ to client app at any time
 #define PUSH_CODE_ADVERT                0x80
@@ -959,6 +960,7 @@ void MyMesh::begin(bool has_display) {
   _store->loadContacts(this);
   bootstrapRTCfromContacts();
   addChannel("Public", PUBLIC_GROUP_PSK); // pre-configure Andy's public channel
+  addChannel("edgewood", EDGEWOOD_GROUP_PSK); // pre-configure private mission channel (idx 1)
   _store->loadChannels(this);
 
   radio_driver.setParams(_prefs.freq, _prefs.bw, _prefs.sf, _prefs.cr);
@@ -2233,6 +2235,13 @@ void MyMesh::loop() {
 #endif
 }
 
+bool MyMesh::sendChannelText(uint8_t channel_idx, const char* text) {
+  ChannelDetails channel;
+  if (!getChannel(channel_idx, channel)) return false;
+  return sendGroupMessage(getRTCClock()->getCurrentTimeUnique(), channel.channel,
+                          _prefs.node_name, text, strlen(text));
+}
+
 bool MyMesh::advert() {
   mesh::Packet* pkt;
   if (_prefs.advert_loc_policy == ADVERT_LOC_NONE) {
diff --git a/examples/companion_radio/MyMesh.h b/examples/companion_radio/MyMesh.h
index 43d3950..a3c51ba 100644
--- a/examples/companion_radio/MyMesh.h
+++ b/examples/companion_radio/MyMesh.h
@@ -98,6 +98,7 @@ public:
   void loop();
   void handleCmdFrame(size_t len);
   bool advert();
+  bool sendChannelText(uint8_t channel_idx, const char* text);
   void enterCLIRescue();
 
   int  getRecentlyHeard(AdvertPath dest[], int max_num);
diff --git a/examples/companion_radio/ui-new/MGRS.h b/examples/companion_radio/ui-new/MGRS.h
new file mode 100644
index 0000000..7f3b888
--- /dev/null
+++ b/examples/companion_radio/ui-new/MGRS.h
@@ -0,0 +1,80 @@
+#pragma once
+#include <math.h>
+#include <stdio.h>
+#include <string.h>
+
+// WGS84 lat/lon (degrees) -> MGRS reference at 1 m precision,
+// e.g. "19TCG0136228411" (15 chars + NUL). Self-contained, no deps.
+//
+// Uses the Kruger/Karney series for the UTM projection (sub-meter accurate),
+// which is far better than needed for reading a 100 m grid square.
+// Not handled: the Norway/Svalbard zone exceptions and polar (UPS) regions —
+// irrelevant between 80S and 84N away from those special zones.
+static bool mgrs_format(double lat, double lon, char* out, unsigned out_sz) {
+  if (out_sz < 16 || lat < -80.0 || lat > 84.0 || lon < -180.0 || lon > 180.0)
+    return false;
+
+  int zone = (int)floor(lon / 6.0) + 31;
+  if (zone > 60) zone = 60;
+
+  static const char bands[] = "CDEFGHJKLMNPQRSTUVWX";
+  int band_idx = (int)floor((lat + 80.0) / 8.0);
+  if (band_idx > 19) band_idx = 19;
+
+  // --- geographic -> UTM (k0 = 0.9996) ---
+  const double a = 6378137.0, f = 1.0 / 298.257223563;
+  const double k0 = 0.9996, E0 = 500000.0;
+  const double n = f / (2.0 - f);
+  const double A = a / (1.0 + n) * (1.0 + n * n / 4.0 + n * n * n * n / 64.0);
+  const double a1 = n / 2.0 - 2.0 * n * n / 3.0 + 5.0 * n * n * n / 16.0;
+  const double a2 = 13.0 * n * n / 48.0 - 3.0 * n * n * n / 5.0;
+  const double a3 = 61.0 * n * n * n / 240.0;
+  const double e = sqrt(f * (2.0 - f));
+
+  double lat_r = lat * M_PI / 180.0;
+  double dlon = lon * M_PI / 180.0 - ((zone - 1) * 6 - 180 + 3) * M_PI / 180.0;
+  double s = sin(lat_r);
+  double t = sinh(atanh(s) - e * atanh(e * s));
+  double xi_p = atan2(t, cos(dlon));
+  double eta_p = asinh(sin(dlon) / sqrt(t * t + cos(dlon) * cos(dlon)));
+  double xi = xi_p + a1 * sin(2 * xi_p) * cosh(2 * eta_p)
+                   + a2 * sin(4 * xi_p) * cosh(4 * eta_p)
+                   + a3 * sin(6 * xi_p) * cosh(6 * eta_p);
+  double eta = eta_p + a1 * cos(2 * xi_p) * sinh(2 * eta_p)
+                     + a2 * cos(4 * xi_p) * sinh(4 * eta_p)
+                     + a3 * cos(6 * xi_p) * sinh(6 * eta_p);
+  double easting = E0 + k0 * A * eta;
+  double northing = k0 * A * xi;
+  if (lat < 0) northing += 10000000.0;
+
+  // --- UTM -> 100 km square letters (MGRS-A lettering scheme) ---
+  static const char col_sets[3][9] = {"ABCDEFGH", "JKLMNPQR", "STUVWXYZ"};
+  static const char row_letters[] = "ABCDEFGHJKLMNPQRSTUV";  // 20-letter cycle
+  int col_idx = (int)floor(easting / 100000.0) - 1;          // cols 1..8 -> 0..7
+  char col = col_sets[(zone - 1) % 3][((col_idx % 8) + 8) % 8];
+  int row_idx = (int)floor(northing / 100000.0);
+  if (zone % 2 == 0) row_idx += 5;                           // even zones offset
+  char row = row_letters[((row_idx % 20) + 20) % 20];
+
+  unsigned long e5 = (unsigned long)floor(easting) % 100000UL;
+  unsigned long n5 = (unsigned long)floor(northing) % 100000UL;
+  char tmp[32];
+  int len = snprintf(tmp, sizeof(tmp), "%d%c%c%c%05lu%05lu",
+                     zone, bands[band_idx], col, row, e5, n5);
+  if (len <= 0 || (unsigned)len >= out_sz) return false;
+  memcpy(out, tmp, len + 1);
+  return true;
+}
+
+// The map-reading pair only: the 100 m grid square as "013-284"
+// (first 3 digits of the easting and northing groups). 7 chars + NUL.
+static bool mgrs_grid100(double lat, double lon, char* out, unsigned out_sz) {
+  char full[16];
+  if (out_sz < 8 || !mgrs_format(lat, lon, full, sizeof(full))) return false;
+  // full is "<zone><band><colrow>EEEEENNNNN" — take the 10-digit tail
+  unsigned len = 0;
+  while (full[len]) len++;
+  const char* tail = full + len - 10;
+  snprintf(out, out_sz, "%.3s-%.3s", tail, tail + 5);
+  return true;
+}
diff --git a/examples/companion_radio/ui-new/UITask.cpp b/examples/companion_radio/ui-new/UITask.cpp
index 7c84201..59c4b40 100644
--- a/examples/companion_radio/ui-new/UITask.cpp
+++ b/examples/companion_radio/ui-new/UITask.cpp
@@ -30,6 +30,7 @@
 #endif
 
 #include "icons.h"
+#include "MGRS.h"
 
 class SplashScreen : public UIScreen {
   UITask* _task;
@@ -92,6 +93,7 @@ class HomeScreen : public UIScreen {
     ADVERT,
 #if ENV_INCLUDE_GPS == 1
     GPS,
+    RECON,
 #endif
 #if UI_SENSORS_PAGE == 1
     SENSORS,
@@ -317,16 +319,32 @@ public:
         sprintf(buf, "%d", nmea->satellitesCount());
         display.drawTextRightAlign(display.width()-1, y, buf);
         y = y + 12;
-        display.drawTextLeftAlign(0, y, "pos");
-        sprintf(buf, "%.4f %.4f",
-          nmea->getLatitude()/1000000., nmea->getLongitude()/1000000.);
+        display.drawTextLeftAlign(0, y, "mgrs");
+        if (!(nmea->isValid() && mgrs_format(nmea->getLatitude()/1000000.,
+                nmea->getLongitude()/1000000., buf, sizeof(buf))))
+          strcpy(buf, "-");
         display.drawTextRightAlign(display.width()-1, y, buf);
         y = y + 12;
-        display.drawTextLeftAlign(0, y, "alt");
-        sprintf(buf, "%.2f", nmea->getAltitude()/1000.);
+        display.drawTextLeftAlign(0, y, "grid");
+        if (!(nmea->isValid() && mgrs_grid100(nmea->getLatitude()/1000000.,
+                nmea->getLongitude()/1000000., buf, sizeof(buf))))
+          strcpy(buf, "-");
         display.drawTextRightAlign(display.width()-1, y, buf);
         y = y + 12;
       }
+    } else if (_page == HomePage::RECON) {
+      LocationProvider* nmea = sensors.getLocationProvider();
+      char grid[12];
+      display.drawTextCentered(display.width() / 2, 18, "grid report -> edgewood");
+      if (nmea && nmea->isValid() && mgrs_grid100(nmea->getLatitude()/1000000.,
+            nmea->getLongitude()/1000000., grid, sizeof(grid))) {
+        display.setTextSize(2);
+        display.drawTextCentered(display.width() / 2, 32, grid);
+        display.setTextSize(1);
+      } else {
+        display.drawTextCentered(display.width() / 2, 34, "(no gps fix)");
+      }
+      display.drawTextCentered(display.width() / 2, 64 - 11, "send: " PRESS_LABEL);
 #endif
 #if UI_SENSORS_PAGE == 1
     } else if (_page == HomePage::SENSORS) {
@@ -447,6 +465,25 @@ public:
       _task->toggleGPS();
       return true;
     }
+    if (c == KEY_ENTER && _page == HomePage::RECON) {
+      _task->notify(UIEventType::ack);
+      LocationProvider* nmea = sensors.getLocationProvider();
+      char grid[12];
+      if (nmea && nmea->isValid() && mgrs_grid100(nmea->getLatitude()/1000000.,
+            nmea->getLongitude()/1000000., grid, sizeof(grid))) {
+        char text[64];
+        snprintf(text, sizeof(text), "grid %s %.6f,%.6f", grid,
+                 nmea->getLatitude()/1000000., nmea->getLongitude()/1000000.);
+        if (the_mesh.sendChannelText(1, text)) {   // idx 1 = 'edgewood'
+          _task->showAlert("Report sent!", 1000);
+        } else {
+          _task->showAlert("Send failed..", 1000);
+        }
+      } else {
+        _task->showAlert("No GPS fix yet", 1000);
+      }
+      return true;
+    }
 #endif
 #if UI_SENSORS_PAGE == 1
     if (c == KEY_ENTER && _page == HomePage::SENSORS) {
@@ -712,6 +749,14 @@ bool UITask::isButtonPressed() const {
 }
 
 void UITask::loop() {
+  // one automatic advert shortly after boot, so nodes exchange
+  // public keys / names without anyone pressing anything
+  static bool boot_advert_sent = false;
+  if (!boot_advert_sent && millis() > 25000) {
+    boot_advert_sent = true;
+    the_mesh.advert();
+  }
+
   char c = 0;
 #if UI_HAS_JOYSTICK
   int ev = user_btn.check();
